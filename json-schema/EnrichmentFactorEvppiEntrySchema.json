{
  "$ref": "#/definitions/EnrichmentFactorEvppiEntrySchema",
  "definitions": {
    "EnrichmentFactorEvppiEntrySchema": {
      "type": "object",
      "properties": {
        "factor_id": {
          "type": "string",
          "minLength": 1
        },
        "evppi": {
          "type": "number"
        },
        "evppi_raw": {
          "type": "number"
        },
        "baseline_max_expected_utility": {
          "type": "number"
        },
        "conditional_max_expected_utility": {
          "type": "number"
        },
        "units": {
          "type": "string"
        },
        "method": {
          "type": "string"
        },
        "regression_degree": {
          "type": "number"
        },
        "n_samples": {
          "type": "number"
        },
        "clamped_low": {
          "type": "boolean"
        },
        "clamped_high": {
          "type": "boolean"
        },
        "noise_floor": {
          "type": "number"
        },
        "status": {
          "type": "string"
        },
        "correlation_active": {
          "type": "boolean"
        }
      },
      "required": [
        "factor_id"
      ],
      "additionalProperties": true,
      "description": "A factor ABSENT from this array was not assessed — a lever an option intervenes on, or a row whose estimator failed (disclosed as FACTOR_EVPPI_PARTIAL on inference_warnings). Absent is NEVER zero and MUST NOT be imputed, ranked, or rendered as \"no value\"."
    }
  },
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$comment": "GENERATED from src/boundary/enrichment.ts by scripts/generate-json-schema.mjs — do not edit by hand; run `npm run generate:json-schema` to regenerate."
}
