{
  "$ref": "#/definitions/EnrichmentOutcomeStatsSchema",
  "definitions": {
    "EnrichmentOutcomeStatsSchema": {
      "type": "object",
      "properties": {
        "mean": {
          "type": "number"
        },
        "std": {
          "type": "number"
        },
        "p10": {
          "type": "number"
        },
        "p50": {
          "type": "number"
        },
        "p90": {
          "type": "number"
        },
        "n_samples": {
          "type": "number"
        },
        "n_valid_samples": {
          "type": "number"
        },
        "validity_ratio": {
          "type": "number"
        },
        "percentiles_source": {
          "type": "string",
          "enum": [
            "samples",
            "unavailable"
          ],
          "description": "'samples' when p10/p50/p90 were computed from actual MC samples; 'unavailable' when no valid samples exist (the stats are then absent). Absence means the producer did not state provenance — consumers MUST NOT assume 'samples'. Never defaulted at any hop, by contract."
        }
      },
      "additionalProperties": true
    }
  },
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$comment": "GENERATED from src/boundary/enrichment.ts by scripts/generate-json-schema.mjs — do not edit by hand; run `npm run generate:json-schema` to regenerate."
}
