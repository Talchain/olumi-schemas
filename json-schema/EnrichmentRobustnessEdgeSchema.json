{
  "$ref": "#/definitions/EnrichmentRobustnessEdgeSchema",
  "definitions": {
    "EnrichmentRobustnessEdgeSchema": {
      "type": "object",
      "properties": {
        "edge_id": {
          "type": "string",
          "minLength": 1
        },
        "from_id": {
          "type": "string"
        },
        "to_id": {
          "type": "string"
        },
        "from_label": {
          "type": "string"
        },
        "to_label": {
          "type": "string"
        },
        "switch_probability": {
          "type": "number",
          "description": "Absence means NOT COMPUTED — never 0 and never 1. A measured 0 is a real measurement and must be preserved. Higher means MORE fragile, so reading absence as 1 fabricates the maximum of the scale. Consumers MUST branch on presence, never coalesce, and MUST omit anything derived from it."
        },
        "severity": {
          "type": "string"
        },
        "marginal_switch_probability": {
          "type": "number"
        },
        "alternative_winner_id": {
          "type": [
            "string",
            "null"
          ]
        },
        "alternative_winner_label": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "edge_id",
        "from_id",
        "to_id"
      ],
      "additionalProperties": true
    }
  },
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$comment": "GENERATED from src/boundary/enrichment.ts by scripts/generate-json-schema.mjs — do not edit by hand; run `npm run generate:json-schema` to regenerate."
}
